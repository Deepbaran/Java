<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context-2.5.xsd">
    <!-- Configuring Doctor class for Spring to manage (IOC) -->
<!--    <bean id="doctor" class="demo.Doctor">-->
        <!-- Inject properties to the elements in the class -->
<!--        <property name="qualification" value="MBBS"></property>-->

        <!-- To inject an object, the bean of that object must be defined for Spring to understand -->
<!--        <property name="nurse" ref="nurse"></property>-->

        <!-- Inject Constructor arguments -->
<!--        <constructor-arg value="MBBS"></constructor-arg>-->
<!--    </bean>-->
<!--    <bean id="nurse" class="demo.Nurse"></bean>-->

    <!-- Telling Spring which base package to scan to find annotated beans (every Component) and load them as Beans -->
    <!-- When defining individual Beans here, we do not need component-scan. If we are not doing so, then we need component-scan to scan all of base package and load Components -->
<!--    <context:component-scan base-package="demo"></context:component-scan>-->
</beans>