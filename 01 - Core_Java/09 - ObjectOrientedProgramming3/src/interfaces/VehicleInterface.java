package interfaces;

public interface VehicleInterface {
	public int getMaxSpeed();
	public void print();
}


/*
 * A interface is basically a class where all it's functions are abstract
 * We don't need to mention abstract keyword explicitly. All the functions are abstract by default
 */